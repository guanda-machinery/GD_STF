<UserControl x:Class="STD_105.OfficePageHost"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:STD_105"
             xmlns:dxlc="http://schemas.devexpress.com/winfx/2008/xaml/layoutcontrol"
             mc:Ignorable="d">
    <Grid>
        <Frame x:Name="newPage" 
               NavigationUIVisibility="Hidden" />
        <Frame x:Name="oldPage"  
               NavigationUIVisibility="Hidden" />

    </Grid>
    <!--頁面顯示區域-->
    <!--
    <ScrollViewer HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Auto" Style="{ StaticResource scrollview_Office }"
        Height="{ Binding OfficeViewModel.PageHostHeight, Source={ x:Static WPFSTD:ViewLocator.Instance } }" 
        Width="{ Binding OfficeViewModel.PageHostWidth, Source={ x:Static WPFSTD:ViewLocator.Instance } }">
        <Grid>
            <Frame x:Name="newPage" NavigationUIVisibility="Hidden" Margin="0,0,15,20"/>
            <Frame x:Name="oldPage" NavigationUIVisibility="Hidden"/>
        </Grid>
    </ScrollViewer>
    -->
</UserControl>
